name: Release

on:
  workflow_dispatch:
    inputs:
      semver_type:
        description: "Semver type"
        required: true
        type: "choice"
        options:
          - "patch"
          - "minor"
          # - "major"
        default: "minor"

jobs:
  main:
    runs-on: ubuntu-20.04
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set envs
        run: echo "::set-output name=node_version::$(cat .nvmrc)"
        id: envs

      - uses: peaceiris/workflows/setup-node@v0.16.0
        with:
          node-version: "${{ steps.envs.outputs.node_version }}"

      - uses: peaceiris/workflows/setup-git@v0.16.0

      - run: npm ci
      - run: npm run build

      - name: Update build assets
        run: |
          git add ./dist
          git commit -m "chore(release): update dist"
          git push origin main

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CURRENT_TAG=$(git describe --abbrev=0)
          NEW_VERSION=$(npm_config_yes=true npx semver ${CURRENT_TAG} --increment ${{ github.event.inputs.semver_type }})
          NEW_TAG="v${NEW_VERSION}"
          RELEASE_TITLE="Release ${NEW_TAG}"
          git tag -a ${NEW_TAG} -m "${RELEASE_TITLE}"
          git push origin ${NEW_TAG}
          gh release create "${NEW_TAG}" --title "${RELEASE_TITLE}" --generate-notes

      - name: Remove build assets
        run: |
          git rm -r ./dist
          git commit -m "chore(release): remove dist"
          git push origin main
